# OpenAI — create a project-scoped key and keep it local.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna

# Dedicated public demo repository only (never a production repository).
GITHUB_PAT=
GITHUB_REPOSITORY=owner/faraday-demo
GITHUB_SEED_REF=main
